# Book Tracker Frontend

React + Vite frontend for Book Tracker. It includes protected routes, JWT storage in localStorage, charts with Recharts and a responsive CSS interface.

## Local Setup

```bash
npm install
npm run dev
```

The frontend expects the API at `http://localhost:8000` by default. Set `VITE_API_BASE_URL` if your backend runs elsewhere.
